import { CustomFunction } from 'dx-core-rtl';

// tslint:disable-next-line: space-in-parens
export const pureHelper: CustomFunction<[any, any?]> = (param1, param2) => {
  // tslint:disable-next-line:prefer-const
  let result;

  // ...

  return result;
};
